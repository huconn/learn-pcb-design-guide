---
import '../styles/global.css';
import type { Topic } from '../lib/content/topics';
import Sidebar from '../components/ui/Sidebar.astro';

interface Props { topic: Topic; activeSubSlug?: string }
const { topic, activeSubSlug } = Astro.props;
---
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{topic.title} · PCB Design Guide</title>
    <meta name="description" content={topic.subtitle} />
  </head>
  <body class="bg-white text-slate-900 antialiased">
    <Sidebar activeSlug={topic.slug} activeSubSlug={activeSubSlug} />
    <div class="lg:pl-64">
      <main class="mx-auto w-full max-w-6xl px-5 sm:px-8 py-8">
        <slot />
      </main>
      <footer class="border-t border-slate-200 mt-16">
        <div class="mx-auto max-w-6xl px-5 sm:px-8 py-6 text-xs text-slate-500 flex justify-between">
          <span>© hardware-guide</span>
          <span class="font-mono">v0.1</span>
        </div>
      </footer>
    </div>
  </body>
</html>
