import TopicLayout from '../layouts/TopicLayout.astro';
import { DECOUPLING_CAPACITOR } from '../lib/content/topics';
import TopicHero from '../components/ui/TopicHero.astro';
import KpiStrip from '../components/ui/KpiStrip.astro';
import TipBanner from '../components/ui/TipBanner.astro';
import Card from '../components/ui/Card.astro';
import ConceptDrawer from '../components/ui/ConceptDrawer.astro';
import ChecklistBlock from '../components/ui/ChecklistBlock.astro';
import ReservoirAnalogy from '../components/concept/ReservoirAnalogy.astro';
import CurrentWaveAnimation from '../components/concept/CurrentWaveAnimation.astro';
import FrequencyBandMap from '../components/concept/FrequencyBandMap.astro';
import TimeDomainScope from '../components/sim/TimeDomainScope.svelte';
import PdnImpedance from '../components/sim/PdnImpedance.svelte';
import MeasurementGallery from '../components/verify/MeasurementGallery.astro';

<TopicLayout topic={DECOUPLING_CAPACITOR}>

<TopicHero topic={DECOUPLING_CAPACITOR} />

<KpiStrip />

<TipBanner>
  <b>힌트</b> — 아래 "Explore Impedance" 카드에서 cap 그룹을 끄고 켜보며 <span class="font-mono text-amber-700">anti-resonance</span> 피크가 어떻게 움직이는지 관찰해보세요.
</TipBanner>

<Card id="explore-impedance" title="Explore Impedance" subtitle="cap 그룹 on/off · 개수 조정 → Z(f)가 실시간 변화. 타겟 임피던스 아래로 유지되는지 확인하세요.">
  <PdnImpedance client:visible />
</Card>

<div class="mt-6">
  <Card id="time-domain" title="Time-Domain Ripple" subtitle="load step에 대한 Vdd 리플. 프리셋을 '0개 (나쁜 예)' → '권장 풀세트'로 바꿔보세요.">
    <TimeDomainScope client:visible />
  </Card>
</div>

<div class="mt-6">
  <Card id="verify" title="Measurement Gallery" subtitle="실측 스코프 캡처 · 0/1/4/4+bulk 비교 (실측 이미지 준비중)">
    <MeasurementGallery shots={[
      { key: 'none', label: '0 caps', caption: 'cap 없음 — 기준' },
      { key: 'one',  label: '1 cap',  caption: '100 nF × 1' },
      { key: 'four', label: '4 caps', caption: '100 nF × 4' },
      { key: 'full', label: '4 + bulk', caption: '100 nF × 4 + 10 μF + 100 μF bulk' },
    ]} />
  </Card>
</div>

<ConceptDrawer>
  <div>
    <div class="font-mono text-[10px] tracking-widest text-slate-500 uppercase mb-2">저수지 비유</div>
    <ReservoirAnalogy />
    <p class="text-sm text-slate-600 mt-1">IC는 샤워기 · VRM은 먼 댐 · PCB 트레이스는 긴 파이프 · cap은 동네 물탱크. 수요가 튀는 순간 물탱크가 먼저 공급.</p>
  </div>
  <div>
    <div class="font-mono text-[10px] tracking-widest text-slate-500 uppercase mb-2">전류 파동</div>
    <CurrentWaveAnimation />
    <p class="text-sm text-slate-600 mt-1">cap이 IC 수요에 먼저 응답 · VRM은 뒤따라옴.</p>
  </div>
  <div>
    <div class="font-mono text-[10px] tracking-widest text-slate-500 uppercase mb-2">주파수 담당</div>
    <FrequencyBandMap />
    <p class="text-sm text-slate-600 mt-1">cap 값이 작을수록 더 높은 주파수를 담당 — 여러 값을 섞어야 전 대역 커버.</p>
  </div>
</ConceptDrawer>

<div class="mt-6">
  <Card id="checklist" title="Design Checklist (TL;DR)" subtitle="실무 리뷰 체크리스트 · 각 항목은 관련 섹션으로 점프">
    <ChecklistBlock items={[
      { text: "각 전원 핀마다 0.1 μF 하나 — IC 바로 옆", anchor: "concept" },
      { text: "다른 값 혼용 (100 nF + 10 μF + bulk)", anchor: "explore-impedance" },
      { text: "via/trace 최단거리 · 같은 층 우선", anchor: "concept" },
      { text: "Ground return 경로 확인", anchor: "concept" },
      { text: "타겟 임피던스 아래 전 주파수에서 유지", anchor: "explore-impedance" }
    ]} />
  </Card>
</div>

<div class="mt-6">
  <Card id="further" title="더 깊이 / 다음 토픽">
    이 가이드의 시뮬레이션은 **교육용 직관 모델** 입니다. 수치는 ±30% 정도만 맞습니다. 실제 설계 검증은 SPICE 와 실측을 꼭 병행하세요.

    - 다음 토픽 (예정): PDN planning, Signal integrity, Ground bounce
    - 피드백/오류 신고: (repo link)
  </Card>
</div>

</TopicLayout>
